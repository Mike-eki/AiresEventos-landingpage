<section class="catering-section">
    <hgroup class="catering-titles-container">
        <div class="catering-titles">
            <h2>AIRES</h2>
            <h2>CATERING</h2>
        </div>
        <img class="catering-bg" src="images/ac-bg.webp" alt="">
        <img class="catering-logo" src="images/ac-logo-white.png" alt="">
    </hgroup>
    <article class="catering-information">
        <header class="catering-text-info">
            <img class="catering-iso" src="images/iso-ac-black.png" alt="">
            <p>Tu evento merece una propuesta gastronómica única, por eso, nuestro equipo de chefs trabaja junto a vos para diseñar un menú que se adapte a tus gustos, estilo y necesidades.</p>
            <p>Desde entradas irresistibles hasta postres que conquistan, cada detalle está pensado para sorprender.</p>
        </header>
        <picture class="chef-frame">
            <img class="chef-image" src="images/ac-chef.webp" alt="">
        </picture>
    </article>

    <ul class="container-of-items">
        <li class="catering-item-container">
            <p class="catering-item catering-item-l catering-round-r c-item-1">Menús diseñados a medida según el tipo de evento.</p>
            <img class="catering-item-img catering-item-img-1" src="images/ac-1.webp" alt="">
        </li>
        <li class="catering-item-container">
            <p class="catering-item catering-item-r catering-round-l c-item-2">Opciones gourmet, tradicionales y de vanguardia.</p>
            <img class="catering-item-img catering-item-img-2" src="images/ac-2.webp" alt="">
        </li>
        <li class="catering-item-container">
            <p class="catering-item catering-item-l catering-round-r c-item-3">Catering adaptable a cócteles, almuerzos, cenas de gala o reuniones informales</p>
            <img class="catering-item-img catering-item-img-3" src="images/ac-3.webp" alt="">
        </li>
        <li class="catering-item-container">
            <p class="catering-item catering-item-r catering-round-l c-item-4">Presentación cuidada al detalle para una experiencia visual y gastronómica impecable.</p>
            <img class="catering-item-img catering-item-img-4" src="images/ac-4.webp" alt="">
        </li>
    </ul>
</section>

<style>
    ul, li {
        list-style: none;
        padding: 0;
        margin: 0;
    }
    img {
        display: block;
        width: 100%;
        object-fit: cover;
    }

    .catering-titles-container {
        position: relative;
        display: flex;
        justify-content: center;
        align-items: center;
    }

    .catering-bg {
        z-index: -1;
        aspect-ratio: 1 / 1;
        filter: brightness(0.5);
        height: 100vh;
    }
    .catering-logo {
        position: absolute;
        top: 1rem; right: 1rem;
        z-index: 10;
        width: 10rem;
    }

    .catering-titles {

        position: absolute;
        display: flex;
        flex-direction: column;
        gap: calc(var(--space) * 1.5);
    }
    .catering-titles h2 {
        color: var(--white);

        text-align: center;

        font-weight: 700;

        --min-font-size: 2.5rem;
        --max-font-size: 6rem;
        --min-viewport: 375px;
        --max-viewport: 1400px;

        font-size: calc(
            var(--min-font-size) + 
            (var(--max-font-size) - var(--min-font-size)) * 
            ((100vw - var(--min-viewport)) / (var(--max-viewport) - var(--min-viewport)))
        );
    }

    .catering-information {
        display: flex;
        min-height: 70vh;
        overflow: hidden;
    }
    .catering-iso {
        width: 100%;
        margin-inline: auto;

        font-weight: 700;

        --min-font-size: 6rem;
        --max-font-size: 18rem;
        --min-viewport: 375px;
        --max-viewport: 1400px;

        max-width: calc(
            var(--min-font-size) + 
            (var(--max-font-size) - var(--min-font-size)) * 
            ((100vw - var(--min-viewport)) / (var(--max-viewport) - var(--min-viewport)))
        );
    }
    .catering-text-info {
        display: flex;
        flex-direction: column;
        gap: calc(var(--space) * 2.5);
        justify-content: center;

        width: 50%;

        padding-block: calc(var(--space) * 1.5)
    }
    .catering-text-info > * {
        padding-inline: calc(var(--space) * 2);
    }
    .catering-text-info p {
        color: var(--desert-sand);
    }
    .chef-frame {
        width: 50%;
    }
    .chef-frame img {
        height: 100%;
        object-position: bottom;
    }

    @media (max-width: 600px) {
        .catering-information {
            flex-direction: column;
        }
        .text-info {
            width: 100%;
        }
        .chef-frame {
            width: 100%;
        }
    }

    .catering-item-container {
        position: relative;
    }
    .catering-item-l {
        top: 0; left: 0
    }
    .catering-item-r {
        top: 0; right: 0;
    }
    .catering-item {
        position: absolute;
        z-index: 100000;
        max-width: 30ch;
        text-wrap: pretty;

        padding-block: calc(var(--space) * 2);
        padding-inline: calc(var(--space) * 2.5);
        background-color: var(--desert-sand);
        color: var(--light-beige);

        --min-font-size: 0.8rem;
        --max-font-size: 1.5rem;
        --min-viewport: 375px;
        --max-viewport: 1400px;

        font-size: calc(
            var(--min-font-size) + 
            (var(--max-font-size) - var(--min-font-size)) * 
            ((100vw - var(--min-viewport)) / (var(--max-viewport) - var(--min-viewport)))
        );
    }
    .catering-item.catering-round-r {
        border-radius: 0 100rem 100rem 0;
    }
    .catering-item.catering-round-l {
        border-radius: 100rem 0 0 100rem;
    }
    .catering-item-img {
        aspect-ratio: 16/9;
    }

</style>

<script>
  import { gsap } from 'gsap';
  import { ScrollTrigger } from 'gsap/ScrollTrigger';
  
  gsap.registerPlugin(ScrollTrigger);
  
  // Animaciones cuando la sección entra en vista
  document.addEventListener('DOMContentLoaded', () => {
    // Animar los títulos de catering - con trigger específico
    gsap.from('.catering-titles h2', {
      scrollTrigger: {
        trigger: '.catering-titles',
        start: 'top 85%',
        end: 'bottom 60%',
        // markers: true, // Descomentar para depuración visual
      },
      opacity: 0,
      y: 50,
      duration: 1.2,
      stagger: 0.4,
      ease: 'power3.out'
    });
    
    // Animar el logo - aparece rápido después de los títulos
    gsap.from('.catering-logo', {
      scrollTrigger: {
        trigger: '.catering-titles-container',
        start: 'top 80%',
      },
      opacity: 0,
      scale: 0.8,
      duration: 0.8,
      delay: 0.2,
      ease: 'power3.out'
    });
    
    // Animar la imagen de fondo - con z-index bajo
    gsap.from('.catering-bg', {
      scrollTrigger: {
        trigger: '.catering-titles-container',
        start: 'top 80%',
      },
      opacity: 0,
      scale: 1.1,
      duration: 1.5,
      delay: 0.1,
      ease: 'power3.out'
    });
    
    // Animar el logo ISO - elemento importante
    gsap.from('.catering-iso', {
      scrollTrigger: {
        trigger: '.catering-text-info',
        start: 'top 85%',
      },
      opacity: 0,
      scale: 0.9,
      duration: 1.2,
      ease: 'power3.out'
    });
    
    // Animar los párrafos de información principal
    gsap.from('.catering-text-info p', {
      scrollTrigger: {
        trigger: '.catering-text-info',
        start: 'top 80%',
      },
      opacity: 0,
      x: -40,
      duration: 1.2,
      stagger: 0.3,
      delay: 0.2,
      ease: 'power3.out'
    });
    
    // Animar la imagen del chef
    gsap.from('.chef-image', {
      scrollTrigger: {
        trigger: '.catering-information',
        start: 'top 80%',
      },
      opacity: 0,
      x: 40,
      duration: 1.3,
      ease: 'power3.out'
    });
    
    // Animar cada texto de item individualmente con triggers específicos
    gsap.from('.c-item-1', {
      scrollTrigger: {
        trigger: '.c-item-1',
        start: 'top 90%',
        // markers: true,
      },
      opacity: 0,
      x: -60,
      duration: 1.1,
      ease: 'power3.out'
    });
    
    gsap.from('.c-item-2', {
      scrollTrigger: {
        trigger: '.c-item-2',
        start: 'top 90%',
      },
      opacity: 0,
      x: 60,
      duration: 1.1,
      ease: 'power3.out'
    });
    
    gsap.from('.c-item-3', {
      scrollTrigger: {
        trigger: '.c-item-3',
        start: 'top 90%',
      },
      opacity: 0,
      x: -60,
      duration: 1.1,
      ease: 'power3.out'
    });
    
    gsap.from('.c-item-4', {
      scrollTrigger: {
        trigger: '.c-item-4',
        start: 'top 90%',
      },
      opacity: 0,
      x: 60,
      duration: 1.1,
      ease: 'power3.out'
    });
    
    // Animar las imágenes de items individualmente con efectos de zoom
    gsap.from('.catering-item-img-1', {
      scrollTrigger: {
        trigger: '.catering-item-img-1',
        start: 'top 85%',
      },
      opacity: 0,
      scale: 1.1,
      duration: 1.3,
      ease: 'power3.out'
    });
    
    gsap.from('.catering-item-img-2', {
      scrollTrigger: {
        trigger: '.catering-item-img-2',
        start: 'top 85%',
      },
      opacity: 0,
      scale: 1.1,
      duration: 1.3,
      ease: 'power3.out'
    });
    
    gsap.from('.catering-item-img-3', {
      scrollTrigger: {
        trigger: '.catering-item-img-3',
        start: 'top 85%',
      },
      opacity: 0,
      scale: 1.1,
      duration: 1.3,
      ease: 'power3.out'
    });
    
    gsap.from('.catering-item-img-4', {
      scrollTrigger: {
        trigger: '.catering-item-img-4',
        start: 'top 85%',
      },
      opacity: 0,
      scale: 1.1,
      duration: 1.3,
      ease: 'power3.out'
    });

    ScrollTrigger.refresh();
  });
</script>