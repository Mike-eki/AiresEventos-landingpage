<section class="intro-text">
    <hgroup>
        <p>A pocos minutos del centro de la ciudad de Rosario, combinamos naturaleza, diseño y excelencia en cada detalle para convertir cualquier ocasión en un recuerdo inolvidable.</p>
    </hgroup>
</section>

<style>
    section {
        background-color: var(--light-beige);
        padding-inline: calc(var(--space) * 2);
        padding-bottom: calc(var(--space) * 4);

        display: flex;
        justify-content: center;
        overflow: hidden;
    }

    hgroup {
        max-width: 90ch;
        border-radius: 100rem;
        border: 4px solid var(--desert-sand);
        color: var(--desert-sand);
    }

    p {
        padding: 3rem;
        text-align: center;
    }
</style>


<script>
  import { gsap } from 'gsap';
  import { ScrollTrigger } from 'gsap/ScrollTrigger';
  
  gsap.registerPlugin(ScrollTrigger);
  
  // Animaciones cuando la sección entra en vista
  document.addEventListener('DOMContentLoaded', () => {
    // Animar el párrafo de texto
    gsap.from('.intro-text hgroup p', {
      scrollTrigger: {
        trigger: '.intro-text',
        start: 'top 80%', // Ajustable a 'top bottom' si se activa demasiado pronto/tarde
        // markers: true, // Descomentar para depuración visual
      },
      opacity: 0,
      y: 30,
      duration: 1,
      ease: 'power3.out'
    });

    ScrollTrigger.refresh(); // Refresca triggers después de carga
  });
</script>