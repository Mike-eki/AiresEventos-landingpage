<section>
    <hgroup>
        <p>A pocos minutos del centro de la ciudad, combinamos naturaleza, diseño y excelencia en cada detalle para convertir cualquier ocasión en un recuerdo inolvidable.</p>
    </hgroup>
</section>

<style>
    section {
        background-color: var(--light-beige);
        padding-inline: calc(var(--space) * 2);
        padding-bottom: calc(var(--space) * 4);

        display: flex;
        justify-content: center;
    }

    hgroup {
        max-width: 90ch;
        border-radius: 100rem;
        border: 4px solid var(--desert-sand);
        color: var(--desert-sand);
    }

    p {
        padding: 3rem;
    }
</style>